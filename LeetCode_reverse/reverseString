基础的双指针字符串反转。。
class Solution {
public:
    void reverseString(vector<char>& s) {
        if (s.empty()) return ;
        int l = 0, r = s.size() - 1;
        while (l < r) {
            swap(s[l ++], s[r --]);
        }
    }
};
